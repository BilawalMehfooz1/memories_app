import 'package:flutter/material.dart';

class MemoriesDetailScreen extends StatelessWidget {
  const MemoriesDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}